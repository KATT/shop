{
  "private": true,
  "scripts": {
    "build": "rimraf dist && tsc",
    "dev": "yarn build -- --watch",
    "start": "yarn start:js && yarn start:nojs",
    "start:js": "yarn nightwatch",
    "start:nojs": "yarn nightwatch --env nojs",
    "test": "yarn start"
  },
  "devDependencies": {
    "@types/nightwatch": "^0.9.7",
    "nightwatch": "^0.9.19",
    "rimraf": "^2.6.2",
    "typescript": "^2.7.2"
  }
}
